Closes #<N>

## Summary

<What changed and why. Three sentences at most. Not a retelling of the issue.>

## Design decision

<Design track: the design in two sentences, plus where the spec lives. Direct track: "Direct track — <the reason it met all four criteria>". Micro track: "Micro track — <the named surface>, promotion <did not fire | fired, and the track it moved to>".>

## Experience

<UI: states shipped; imagery (or a no-art note); motion / a11y; verified at ~360×640 and in both themes. Otherwise: "N/A — no user-facing UI".>

## Verification

```
<command>   → <actual result, e.g. "42 tests passed">
<command>   → <actual result>
```

## Left undone

<A list of what is deferred or out of scope. "Nothing" only when true.>
